// Central place for the app's display name. Referenced by the nav header,
// the browser tab title (see main.jsx), etc. — change it here rather than
// hunting down individual strings.
export const APP_NAME = 'Logero'
