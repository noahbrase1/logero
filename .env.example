VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Public VAPID key for Web Push (safe to expose — pairs with a private key
# that lives only as a Supabase Edge Function secret, never here).
# Generate a pair with: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=
